<%= simple_form_for @user do |user| %>
  <%= user.input :name %>
  <%= user.input :bio %>
  <%= user.input :photo, as: :attachinary %>
  <%= user.button :submit, class: 'btn btn-turqoise' %>
<% end %>
